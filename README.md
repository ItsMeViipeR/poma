# poma

**Poma** (*POrt MAnager*) is a CLI tool to view and kill processes listening on network ports.

## Features

- View all open ports and their associated processes.
- Quickly kill the process using a specific port.

## Usage

List all open ports:
```bash
poma
```

Kill the process running on a specific port:

```bash
poma <port>
```
